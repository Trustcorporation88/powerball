import { ProFeatureCard } from '@/components/ProFeature';

export default function CalculatedColumns() {
  return (
    <ProFeatureCard
      title="Colunas Calculadas"
      description="As colunas calculadas avançadas ficam disponíveis apenas para Usuário Pro até a conclusão do motor seguro, persistência e auditoria completa dessas fórmulas."
    />
  );
}
